import { PagePlaceholder } from "./PagePlaceholder";

export function LoginPage() {
  return (
    <PagePlaceholder
      title="ログイン"
      description="Supabase Auth を使ったログイン画面をここに実装します。"
    />
  );
}
